chicken-egg.scm is the wrapper file containing Chicken Scheme
specific code.

main.scm is the wrapper file containing support for running scheme
code as an executable program.

Everything else here is intended to run on other suitably featured
Scheme implementations.
